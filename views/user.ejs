<!DOCTYPE html>
<html>
  <head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <link rel="stylesheet" href="/style.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.100.2/css/materialize.min.css">
  <title>User Page</title>
  </head>

  <body>
    <nav>
    <div class="nav-wrapper">
      <a href="#" class="brand-logo">Reddit</a>
      <ul id="nav-mobile" class="right hide-on-med-and-down">
        <li><a href="/users">All Users</a></li>
      </ul>
    </div>
  </nav>
    <div class="container">
    <h1>Welcome, <%= user.username %>!</h1>
    <form action="/users/<%= user.id%>"method="post">
      <input type="text" placeholder="title" name="title">
      <input type="text" name="post" placeholder="post">
      <input type="submit" >
    </form>
  <h2>Your Posts</h2>

    <% for(var i =0; i < posts.length; i++) { %>
      <div class="row">
        <div class="col s12 m6">
          <div class="card blue-grey darken-1">
            <div class="card-content white-text">
              <span class="card-title">Title: <%= posts[i].title %></span>
              <p>Post: <%= posts[i].post %></p>
            </div>
            <div class="card-action">
            <form action="/users/<%= user.id%>/posts/<%=posts[i].id%>/comments" method="post">
             <input type="text" name="comment">
             <input type="submit">
            </form>
            <p>Comments</p>
            <ul>
              <% for(var j = 0; j < comments.length; j++){ %>
                <% if(comments[j].post_id == posts[i].id ) { %>
                <li><%= comments[j].comment %></li>
                <% } %>
              <% } %>
            </ul>
            </div>
            <div class="card-action">
              <a href="#">This is a link</a>
              <a href="#">This is a link</a>
            </div>
          </div>
        </div>
      </div>
    <% } %>
    <script src="https://code.jquery.com/jquery-3.2.1.min.js" integrity="sha256-hwg4gsxgFZhOsEEamdOYGBf13FyQuiTwlAQgxVSNgt4=" crossorigin="anonymous">
     </script>
 <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.100.2/js/materialize.min.js"></script>
</body>
</html>
